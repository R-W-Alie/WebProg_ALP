create table
    Employees (
        employee_id int(10) AUTO_INCREMENT not null,
        first_name varchar(20) not null,
        last_name varchar(20) not null,
        email varchar(100) not null,
        phone_number varchar(20) not null,
        hire_date date not null,
        job_id int (8),
        foreign key (job_id) references Jobs(job_id),
        salary decimal(10,3) not null,
        manager_id int(8),
        foreign key (manager_id) references Employees(employee_id),
        department_id int(8),
        foreign key (department_id) references Departments(department_id),
        primary key (employee_id)
    );

-- create table
--     order_details (
--         order_detail_id int(10) AUTO_INCREMENT not null primary key,
--         order_id int(10) not null,
--         foreign key (order_id) references orders(order_id),
--         product_id int(10) not null,
--         foreign key (product_id) references products(product_id),
--         price decimal(10,2) not null
--     )

create table
    orders (
        order_id int(10) AUTO_INCREMENT not null primary key,
        user_id int(10) not null,
        foreign key (user_id) references users(user_id),
        order_date datetime not null,
        status_order ENUM('PAID', 'DELIVERD', 'OUT FOR DELIVERY') not null,
        quantity int(10) not null,
        total_price int(10) not null,
        status ENUM('SELECTED','NOT SELECTED') NOT NULL 
    );

create table
    products (
        product_id int(10) AUTO_INCREMENT not null primary key,
        product_name varchar(50) not null,
        price int(10) not null,
        stock int(10) not null,
        description text default null,
        image varchar(1000) not null 
    );

create table
    users (
        user_id int(10) AUTO_INCREMENT not null primary key,
        name_user varchar(50) not null,
        email varchar(100) not null,
        no_hp varchar(20) not null,
        address varchar(255) not null,
        password varchar(100) not null
    );